# Authorized B13/B14 local bundle

September 18, 2026. Coordinator dispatch after B12 `cf4b0ee`, under the user's standing instruction to advance work independent of Design Redux. This is the durable execution boundary, not production or reskin approval.

## Isolation and permitted work

Use a separate checkout **and independent node_modules**. The B11 review snapshot's dependencies link to the prior checkout: do not modify them. Preserve preview app 3445/services 562xx, build `19acbce`, every fictional user edit and all prior artifacts. No preview stop/rebuild/reseed/migration. Ports 56321/56322/56324 and 3455 are proposed for the new minimal source/review stack; 56421/56422/56424 and 3465 for a fresh restore destination. Confirm availability. Run only needed services, retain volumes after cleanup. Do not alter unrelated dirty work in the original checkout.

B13: pin candidate/build/schema/config, inventory every implemented route/RPC/table/asset/service trust boundary and map test/inspection evidence. Reuse applicable B01–B12 results; fresh checks focus on composition, genuine session/MFA/tenant/role admission, direct API versus privileged helpers, template/source/share restrictions, lifecycle freeze and imports, unknown outcomes/account switching, input/output/injection, secrets/dependencies, logs/cache/origin/CSRF/referrers and local quota behavior. Check disabled/unavailable surfaces explicitly. Fix demonstrated blockers in isolated commits, retest affected checks, preserve failures. No speculative feature expansion or external scanner installation. Do not start dependent B14 with an unresolved blocking finding; continue independent review.

Original B08 exception stays exactly same-token/same-verified-account concurrent sessions. Its original failed assertions and exit status remain preserved; no different-account, tenant, MFA or other exception is inferred. The paused provider/token-redemption race investigation and equivalent probes are excluded.

Commit the B13 reviewed candidate and ledger with the label **implemented-surface local review; exceptional recovery and operational purge absent/pending; hosted controls unverified**. No blanket B13/F01–F05/release acceptance. Later implementation of excluded features requires B12/B13/B14 delta work.

## B14 executable procedure

1. Rehearse the last B13-reviewed compatible application with the current schema. If the database is unavailable, recover that schema and complete latest state into a fresh connected local stack. Legacy remains read-only. This is approval of a fictional same-schema recovery method, not selection of actual production D09, schema downgrade or reverse export to Netlify.
2. Reuse B12 original inputs/mappings and E10's immutable checkpoints, ordered journals, hashes, predecessor checks and failure patterns. Build a separate genuine-Auth fixture. Inventory current application/Auth identities/factors/sessions, memberships/restrictions, history, snapshots/template provenance, shares/aliases, lifecycle, captures, audit/outbox and operation receipts/ledgers. Include migration history, extensions, role/ownership/default grants, RLS, PostgREST admission hook and required Auth/service/config/secret dependencies. Secret-bearing fictional restore artifacts stay owner-only and out of Git/logs.
3. Capture a baseline, make genuine newer fixture edits across relevant stores and authorization/lifecycle state, freeze/drain source writes, capture the final journal, and prove source denials. Restore disk artifacts into a fresh connected Auth/PostgREST destination, frozen until reconciliation and admission checks pass. Missing hook/config/grant material rejects activation.
4. Inject partial-load and missing-config failures; retain failed generations. Prove atomic rollback, exact lost-ack/retry, wrong predecessor/reordered journal refusal, and newer destination edits protected. Verify genuine password/MFA, role/session boundaries, revoked shares, pending-deletion freeze and current application/API document/template/snapshot/CMS/Client reads.
5. Activate only the reconciled destination, keep the source denied, make a new genuine destination edit, freeze/capture it, then rebuild a second fresh generation entirely from saved files. Require zero unexplained missing/extra/hash/version/history differences and recovery of that newest edit. Measure local restore duration and write an operator procedure.
6. Commit B14 separately. Stop batch services, retain source/destination volumes and artifact generations; leave the designated B11 preview running. Label **local connected same-schema fallback rehearsal; production D09, hosted RPO/RTO and excluded-feature deltas pending**.

## Explicit exclusions and pending decisions

No exceptional factor reset/purge implementation, hosted change, actual accounts/data/mail/source export/migration, push/deploy, purchase, design/branding change, provider investigation or equivalent race probes. No new subagent/executor. Notify coordinator at the B13 checkpoint and promptly for preview-impacting issues.

Queue remaining decisions at normal gaps; independent work proceeds. Purge deferral is not assumed: recommendation for later release still needs approval, retains honest frozen awaiting_purge and the permanent-deletion obligation, and must cover dependencies/shared accounts, retention/legal holds, resumability, backup/restore reapplication and an operations owner before production release. Exceptional recovery remains a pre-B15/B16 gate pending the one-use expiring actor/org/target-bound replacement-only mechanism, factor/session revocation, exact retry, audit/notifications and restricted cross-organization/platform impact contract. Existing approvers/proof and creative decisions stay settled. D06 release admission acceptance, actual D09 fallback/owners/custody/RPO/RTO, D07 clients and hosted/baseline permissions remain later gates.

Observed B11/preview/B12 commit intervals were about 59/5/18 minutes, not measured execution times. They do not establish a connected-restore ETA or guarantee the 18-hour reskin target. Report measured B14 local restore time and current evidence limits.
