# Migration critical path — pilot to cutover

> **September 16 current-state reconciliation:** The editor, local account workspace, lifecycle recovery, invitations/acceptance and restricted provisioning are now implemented and tested locally at `379e066`. Older missing-feature statements below are historical. Full related application integration, account recovery, security/hosted parity and connected rollback still remain. See [TECHNICAL_ACCEPTANCE_AUDIT.md](./TECHNICAL_ACCEPTANCE_AUDIT.md) for evidence, gates and bounded dispatch proposals.

> **Related-store implementation:** shared snapshot/template/library/CMS preservation, stable catalogue mapping, import history, catch-up and per-record reconciliation are now tested locally. This closes the prior inventory-only coverage gap at the private migration-catalogue layer, not at the application/UI layer. See [RELATED_MIGRATION_CONTRACT.md](./RELATED_MIGRATION_CONTRACT.md) and [READINESS.md](./READINESS.md). Next: local-only authenticated account/library/editor integration and explicit related application projections. Real migration remains blocked by the parity and complete-export gates.


> **TOTAL PARITY GATE (2026-09-15): NOT PASSED.** Before beginning migration of any real schedules, verify total parity between actual current Netlify production behavior and the eventual Supabase/Vercel target runtime, then obtain explicit user approval. This includes PDF creation, printing, navigation, data entry, input-field sizing and every existing feature. Local SQL/API tests or Netlify staging results cannot satisfy this gate. Each item requires Pass/Fail/Not tested and reproducible comparison evidence; unknown/untested blocks migration. Verified improvements may Pass without copying old pixels or needing waivers solely for differing; regressions and untested required behavior block migration. Surface material workflow tradeoffs and unclear cases for user review. Synthetic rehearsals may continue within their existing authorization. See [REGRESSION_MATRIX.md](./REGRESSION_MATRIX.md).


> **September 15 local update:** genuine password-login/API verification, recoverable schedule lifecycle contracts and a bounded synthetic import/delta-reconciliation rehearsal now pass on `codex/migration-schedule-contract`. See [READINESS.md](./READINESS.md) for evidence and [MIGRATION_REHEARSAL.md](./MIGRATION_REHEARSAL.md) for limitations. Full related-store migration and hosted/editor integration remain open. Final acceptance requires Netlify availability and complete per-schedule verification against the latest frozen source state under [MIGRATION_RUNBOOK.md](./MIGRATION_RUNBOOK.md); no live action or shutdown is authorized.


Assessed 2026-09-14 from development `b9d6523`, current source, GitHub PR metadata and dated verification records. No hosted migration, new deployment, production data operation or infrastructure removal was performed. Historical test results below were not rerun for this assessment.

## Local implementation update — September 14

The foundation integration and narrow schedule read/update contract have since been completed and tested in the isolated local branch `codex/migration-schedule-contract`; see [READINESS.md](./READINESS.md) and [SCHEDULE_CONTRACT.md](./SCHEDULE_CONTRACT.md). The integrated chain passed 257 database assertions and 11 real concurrency scenarios. Remote draft metadata and earlier gaps below describe the pre-implementation assessment. No push, hosted migration or cutover occurred. Continue from the tested local branch; next work is target-runtime JWT/adapter verification, remaining lifecycle/repository/account integration and full import/reconciliation.

## Priority

Migration readiness is the near-term critical path: foundation → data adapters/import/reconciliation → accounts and permissions → protected Supabase-backed Vercel pilot → controlled cutover. Installation/offline remains ahead of later Production Command expansion, but must not displace this work or become a new cutover requirement. Small installation validation can proceed alongside migration when it does not consume the capacity needed for the critical path. Offline delivery can follow migration; broader production-management expansion remains later.

## Evidence versus remaining gates

| Stage | What exists / evidence | Remaining blocker and next observable gate |
|---|---|---|
| 1. Integrated foundation | Tenant/auth SQL and server organization/invitation workflows exist. PR #5 is currently open/draft at `b9d6523` and mergeable. Local lifecycle hardening has recorded 148 pgTAP assertions plus eight concurrency scenarios; the separate schedule draft has recorded 159 assertions. | PR #8 remains open/draft at `3e0cb89`, now **CONFLICTING** with its auth base. Integrate the schedule foundation with current dependency/CI/lifecycle changes in an isolated branch; preserve current fixes. Existing separate suites do not prove the combined schema. Require a clean disposable migration chain, unified role/lifecycle/concurrency tests and CI. |
| 1a. Safe schedule mutations | Schedule draft defines phases, days, stable IDs, JSONB documents, RLS and immutable document history. Legacy Netlify save guards are tested separately. | Supabase writes do not mandate an expected version; direct UPDATE can omit it. Nested validation is only a container/size check; metadata/history restoration scope is incomplete. Deleted-row UPDATE policies prevent recovery; active-parent and insert/attribution cases need enforcement. Add a validated, permission-checked transactional mutation boundary, block bypass, and test stale/omitted versions, concurrent writes, malformed documents, deleted parents and authorized recovery. |
| 2. Data layer and migration tooling | Existing API modules provide a useful seam but call `/.netlify/functions/*`. The tested backup/import round-trips schedule documents through Netlify. | No working Supabase editor repository or complete Blob-to-Postgres importer/reconciliation was found. Current backup excludes snapshots, templates, library metadata and CMS; it is not the migration exporter. Implement adapters, stable legacy mappings, repeatable all-store export, idempotent dry-run/import and count/checksum/relationship reconciliation. Prove reruns and recovery against synthetic fixtures before approved real-data import. |
| 3. Accounts and permissions in the app | SQL bootstrap/invitation acceptance, server provisioning/invitation administration and a disabled setup page exist. September lifecycle fixes cover suspension and deleted organizations locally. | Daily login still uses shared-password HMAC/sessionStorage; there is no complete Supabase login/recovery/invitation/session/organization UI connected to the editor. Implement that path, current membership checks and failure cases, plus remaining workflow replay/rate-limit tests. Complete target shares, projection/revocation and applicable private asset policies; offline/local authentication is not required for online migration. |
| 4. Protected Supabase-backed Vercel pilot | Netlify staging `bfa59a2` passed recorded 77 application/platform tests, hosted save/conflict/link/template/backup checks and GitHub application/database CI. The later browser Restore walkthrough succeeded after manual confirmation, persisted after reload, and the fictional fixture's recorded visible starting values were recovered and rechecked. | This staging app still uses Netlify Blobs and shared-password auth. It proves legacy reliability, not Vercel/Supabase runtime integration. Provision/configure the planned protected target only within separately authorized hosted work; apply the reviewed schema there, connect accounts/adapters, replace or port all required Netlify function paths (including library, snapshots, templates, CMS and links). A Vercel front end proxying live Netlify is not completion. Test actual target runtime, full daily workflow, mobile/print, permissions, expiry/revocation and backup/rollback. |
| 5. Controlled cutover | `MIGRATION_RUNBOOK.md` defines invariants, reconciliation, a 1–2 hour read-only window, 12-month route compatibility and a 30-day rollback stability period. | No reconciled target pilot, production inventory/import, measured rehearsal, final backup, legacy-link migration, target-write recovery ledger or cutover sign-off exists in the evidence reviewed. Rehearse first, record owners/timing, stop legacy writes, reconcile final delta, validate and switch only after explicit rollout approval. Keep one writer and preserve legacy data; retirement is separate. |

## Smallest next implementation task

Prepare a local integration branch combining current auth/readiness work with the schedule-domain draft, resolving the existing PR conflict without reverting dependency, CI, lifecycle or editor fixes. Run the combined migrations and database tests in a disposable local environment. Deliver a reviewed, reproducible foundation baseline; do not merge to main or apply it to hosted Supabase as part of that task.

Then implement one narrow schedule read/update mutation contract on that baseline: valid fixture read/write succeeds, omitted/stale versions fail, two concurrent writers produce one winner, unauthorized/deleted-parent writes fail, and document metadata/history stay consistent. This unlocks the Supabase repository slice. Complete restore/lifecycle coverage before pilot acceptance; do not wire production writes around known gaps. Export inventory/fixture design can proceed independently once its full data scope is explicit.

## Scope choices and dated evidence

- The September 11 roadmap's initial dependency failures and absence of fresh local database checks are superseded by later September 11–12 remediation/validation in `READINESS.md`. They are not still-open work on the updated auth branch. PR #8's old dependency files and August-only status checks still need reconciliation with the current base.
- Hosted Supabase was last schema-checked on September 11/12 with the five original auth migrations only. The August schedule migration and September lifecycle migration were not applied there. This assessment did not refresh hosted schema state; apply no migration based solely on dated records.
- Existing Netlify browser Restore is now verified with manual confirmation. The separate native snapshot-label prompt incompatibility remains; neither result proves Supabase restore semantics or blocks source-level foundation integration.
- Preserve the daily editor and hybrid JSON schedule document. Full contacts/locations normalization, realtime collaboration, billing, native packaging and offline editing are not required to leave Netlify.
- The roadmap currently requires neutral theming before pilot. For the fastest **Roseland-only** migration, a specific product decision is whether to retain current Roseland styling and defer neutral/new-organization customization until external onboarding. Do not silently remove that existing gate. This decision does not block foundation integration.
- Vercel remains the documented target preview. Choosing the first installable device/package is independent and does not block the migration foundation. No reliable cutover date can be given until the integrated foundation, importer reconciliation and timed target rehearsal exist.

## Sources

Current source: `lib/api/*.ts`, `lib/store/authStore.ts`, `lib/platform/*`, current `supabase/migrations/`, and schedule SQL at `3e0cb89`. Evidence: [READINESS.md](./READINESS.md), [STAGING_VERIFICATION.md](./STAGING_VERIFICATION.md), [MIGRATION_RUNBOOK.md](./MIGRATION_RUNBOOK.md), plus the completed September 14 browser verification. GitHub metadata checked for [auth PR #5](https://github.com/Jovestar33/roseland-schedule/pull/5) and [schedule PR #8](https://github.com/Jovestar33/roseland-schedule/pull/8). Older staging notes describing the browser Restore check as pending are superseded by this assessment's dated result.


## Netlify billing-transition milestone

- [ ] After complete final active-schedule/related-data reconciliation, a functioning Supabase/Vercel pilot, approved cutover, successful real-use/rollback review and the agreed parallel availability period, notify the user when reducing or ending the **paid Netlify plan** is appropriate.
- [ ] Verify current provider/account plans and compare combined Vercel + Supabase costs and residual Netlify dependencies. Assess a downgrade that retains required Netlify fallback/legacy access; never assume cancellation is compatible with availability.
- [ ] Present the evidence, proposed cost/effective date and any unresolved availability tradeoff for an explicit user decision. No subscription change, purchase or shutdown is authorized.

The existing daily Supabase follow-up includes this future notification gate. Details are in [MIGRATION_RUNBOOK.md](./MIGRATION_RUNBOOK.md). Billing reduction remains downstream of migration acceptance, not a new blocker for today's local work.
