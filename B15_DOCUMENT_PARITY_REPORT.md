# Document-workflow continuation

Work in progress, September 19, 2026. Based on frozen checkpoint `22b77d7`; completed preview3514 remains unchanged. This checkout is `/private/tmp/roseland-b15-document-parity`, branch `codex/b15-document-parity`. The isolated test preview uses3516 and optional scoped fault proxy3515. Its initial build is the inherited `bYu7a1qaFvI5AErNSarin`.

## Verified additional slices

One new fictional schedule, `1b150e87-7959-4ab8-8e23-76970ca6a928`, was created through the normal UI in the existing B14 production. Original retained schedules were not deliberately saved or modified. Existing memberships, credentials, templates and access grants were retained.

- A named snapshot capture genuinely committed while the candidate proxy replaced its acknowledgement with503. Check snapshot result recovered the original capture without another capture, preserving newer dirty project metadata, date, description and notes. The original target stayed at saved version2 until a later deliberate Save.
- At390×844, both Restore/Cancel and Apply/Keep-current actions were keyboard reachable. Canceling snapshot Restore and template Apply retained the exact visibly checked dirty project name, date and row content. The final deliberate save made version3 contain those edits.
- Applying the existing `B14 newest template name` and saving made version4 retain the target metadata and template rows. Database readback confirms its inherited template binding includes the original user-specific export denial. No direct restriction or membership mutation was performed. The affected existing recipient is suspended and already denied before application, so these genuine-session results do not isolate the inherited denial as their cause.

Evidence is in `evidence/b15-document/cases/`. The original source restriction remains unchanged; inherited restrictions live in `private.schedule_template_bindings`, not duplicated public restriction rows. `template-binding-preservation.json` records the correct storage location and recipient limitation.

## Demonstrated feedback defect; candidate fix pending validation

The lost snapshot acknowledgement displayed `Schedule request failed` even though capture had committed. A source-only change replaces that transport-failure message with an uncertain-result explanation and directs the user to Check snapshot result. No request recovery, authorization or persistence semantics were changed. This edit has not yet been built or tested and is not part of frozen preview3514.

## File-chooser interruption and renewed window

The checksum-file chooser call took45936.1494 seconds to return. The next clock reading was22:00:19 UTC, past the original11:50:49 UTC deadline. The browser showed session expiry and retained unfinished work. Import rejection, collision and recovery checks were therefore not counted as completed. No import confirmation was issued. All three generated fictional input files still match their original hashes.

The test preview and proxy were stopped, the browser tab was marked for handoff, and the known unsaved changes were recorded separately in `pending-draft-reconstruction.json`; that file explicitly is not a fresh browser-state export. The latest authenticated target readback is version4 in `after-template-apply.json`.

The user then renewed authorization through September20 at02:00:42 UTC. Continue by validating the candidate wording, recovering the genuine session normally, and completing the unfinished document/import checks. Passing earlier runtime save/navigation/filter/input evidence is reused; no repeated coordinator transmission is authorized or needed. Full human, hosted and physical-device acceptance remains open.
